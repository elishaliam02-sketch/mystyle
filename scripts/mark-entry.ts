/** The bridge the icon generator bundles: the mark and the palette, as data. */
import {
  MARK_PEAK,
  MARK_PEAK_WIDTH,
  MARK_STEP,
  MARK_STEP_WIDTH,
} from "@/theme/mark";
import { palette } from "@/theme/tokens";

export const MARK = {
  peak: MARK_PEAK,
  peakWidth: MARK_PEAK_WIDTH,
  step: MARK_STEP,
  stepWidth: MARK_STEP_WIDTH,
};

export const COLORS = palette;
